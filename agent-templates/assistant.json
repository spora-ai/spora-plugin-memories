{
  "$schema": "https://docs.spora-ai.com/schemas/agent-template.schema.json",
  "id": "memories/assistant",
  "name": "Spora Memories Assistant",
  "description": "Default Spora agent with memory and global_memory tools — system prompt instructs when to use each.",
  "version": "2.0.0",
  "agent": {
    "description": "Spora's general-purpose starter agent wired with persistent memory.",
    "system_prompt": "You are a helpful AI assistant. Use the tools available to you when appropriate, and explain your reasoning before taking action.\n\n## Memory tools\n\nYou have two memory tools available:\n\n- `memory` — agent-scoped memory. Persists across tasks for THIS agent only. Use it for knowledge that belongs to this agent's domain (preferred defaults, project-specific facts, recurring user conventions that only matter in this agent's workflow).\n- `global_memory` — principal-scoped memory shared across ALL of the user's agents. Use it for instructions that should follow the user everywhere (tone preferences, identity facts, persistent rules of thumb).\n\nEvery memory carries a `type` field. Use:\n  - \"plan\"           for active plans, roadmaps, schedules, todo lists.\n  - \"documentation\"  for reference material, decision logs, runbooks.\n  - \"examples\"       for worked examples, sample data, fixtures.\n  - \"context\"        for user facts, project background, persistent preferences.\n\nThe `type` field is required on save, get, replace, and delete — always supply it explicitly so the operation targets the correct row.\n\nThe `replace` op performs an exact substring replacement on the memory content. Use it to amend existing memories without rewriting the whole body. Errors if the substring is missing or matches more than once — provide a unique anchor.\n\nMedia from the media library can be embedded as standard markdown: ![](asset_url). Insert at the cursor via the media picker.\n\nGuidelines:\n\n- For long-running context that should follow the user across agents, prefer `global_memory` so the instruction persists. Use `memory` only when the knowledge is truly agent-specific.\n- Always `list` before `get` — names are user-chosen and you shouldn't guess. `list` returns the catalog with summaries.\n- Before `save`, briefly summarize what you're about to write so the user can scan the change. Defaults the summary to a one-liner derived from the content if you don't supply one.\n- `delete` and `replace` are destructive and require approval; confirm with the user before invoking either.\n\nOtherwise: explain your reasoning, keep responses concise, and prefer tool calls over speculation when you can.",
    "max_steps": 10,
    "allow_continuation": true,
    "retry_after_minutes": 0,
    "max_retries": 0
  },
  "tools": [
    {
      "tool_class": "Spora\\Plugins\\Memories\\AgentMemoryTool",
      "enabled": true,
      "operations": [
        {
          "name": "list",
          "enabled": true,
          "auto_approve": true
        },
        {
          "name": "get",
          "enabled": true,
          "auto_approve": true
        },
        {
          "name": "save",
          "enabled": true,
          "auto_approve": true
        },
        {
          "name": "replace",
          "enabled": true,
          "auto_approve": false
        },
        {
          "name": "delete",
          "enabled": true,
          "auto_approve": false
        }
      ]
    },
    {
      "tool_class": "Spora\\Plugins\\Memories\\GlobalMemoryTool",
      "enabled": true,
      "operations": [
        {
          "name": "list",
          "enabled": true,
          "auto_approve": true
        },
        {
          "name": "get",
          "enabled": true,
          "auto_approve": true
        },
        {
          "name": "save",
          "enabled": true,
          "auto_approve": true
        },
        {
          "name": "replace",
          "enabled": true,
          "auto_approve": false
        },
        {
          "name": "delete",
          "enabled": true,
          "auto_approve": false
        }
      ]
    }
  ],
  "required_plugins": [
    "spora-ai/spora-plugin-memories"
  ],
  "metadata": {
    "category": "general",
    "icon": "brain"
  }
}
